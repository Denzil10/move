## 2024-11-20 - Missing ARIA Labels on Icon Buttons\n**Learning:** This desktop app heavily relies on icon-only buttons for interactive elements (like '×' for closing toasts or '💬' for chat) which are mostly missing screen reader support.\n**Action:** Add `aria-label` properties to all icon-only buttons across components to ensure baseline accessibility without breaking visual design.
