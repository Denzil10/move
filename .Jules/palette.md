## 2026-09-06 - Added ARIA labels to close buttons
**Learning:** The Move Pet application heavily relies on icon-only close buttons ('×' or '&times;') for toasts and overlays, which are visually clear but lack screen reader accessibility.
**Action:** Applied aria-label='Close' to all instances and will verify new components include them.
