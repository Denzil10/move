## 2024-05-18 - Missing ARIA Labels on Close Buttons\n**Learning:** The desktop frontend frequently uses custom toast and overlay components with icon-only close buttons (×) that completely lack `aria-label` attributes, creating a pervasive screen reader accessibility barrier.\n**Action:** Always verify new custom overlays and toasts in this repository include explicit `aria-label="Close"` on their dismiss buttons.
