## 2024-05-24 - Missing ARIA labels on close buttons
**Learning:** Found multiple toast and overlay components with icon-only close buttons lacking accessibility descriptions (`aria-label`).
**Action:** Adding `aria-label="Close"` to these buttons to ensure screen readers provide meaningful context for users.
